# Handoff: candidature-et-reservation

For the next session — human or agent — what to do first and what stands in the way.
Rewritten, not appended, at every stage stop; a stage that STOPs mid-way writes it before it
stops, so nothing is carried in anyone's head.

## Next steps

1. Merged and archived; nothing to pick up. The next stub of the batch is `frais-de-service`, which inserts the payment before `acceptAnswer` in `src/lib/reservations/bookings.ts`.

## Blockers

- none

## Do not

- Do not reopen this run; the four parked findings are triage stubs (`reservations-*`) for their lanes.

## Made in this run

- D-70 to D-78 (Define) and D-82 to D-86 (Build) — see `decisions.md`.
