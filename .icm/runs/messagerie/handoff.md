# Handoff: messagerie

For the next session — human or agent — what to do first and what stands in the way.
Rewritten, not appended, at every stage stop; a stage that STOPs mid-way writes it before it
stops, so nothing is carried in anyone's head.

## Next steps

1. The operator smoke-tests the preview https://berceo-git-claude-tender-albattani-vje2w4-kodominio.vercel.app : as a family with a request, and a validated professional in its commune who answers it — both see Berceo's amorce, exchange messages, each gets one e-mail per message, « Lu » and the « Messages » count follow; booking adds Berceo's « excellente garde »; a conversation on a past night shows the closed line.
2. The operator ticks **Ready to merge** in the body of https://github.com/k0d0minio/berceo/pull/44, then `/pipeline release messagerie`.
3. Release reads `03_build/output/notes.md` → Notes for Release first: the family's first name shown to the professional before a booking is flagged there for the operator.

## Blockers

- blocked on operator: smoke the preview and tick **Ready to merge** on https://github.com/k0d0minio/berceo/pull/44.

## Do not

- Do not tick either gate box, or the acceptance-criteria boxes in the PR body (their status is in `notes.md`).
- Do not add a second migration on this branch; 0008 is the run's one.
- Do not subscribe to PR activity on #44 (`.icm/_shared/github.md` → PR events).
