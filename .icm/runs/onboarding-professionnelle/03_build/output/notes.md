# Build notes: onboarding-professionnelle

- commits: b9b8d36 schema + migration 0002 · 05c0df8 commune register · b1fa5b5 rules, storage, file route, settings · afe4b9d catalogue · 9538545 pages, forms, uploads, admin switch · 48e5a59 docs
- ci: settled after the ready flip (see status.md)

## What changed

- `src/db/schema.ts`, `drizzle/0002_onboarding_professionnelle.sql`: five enums, `professional_profiles` (CHECKs: rate 100–300, bio ≤ 500, INAMI 11 digits), `professional_communes`, `professional_documents`, `professional_declarations`, `app_settings`. Applied on the run's Neon branch `run/onboarding-professionnelle` with `db:migrate` + `db:verify` (3 of 3 journal entries); a live insert there refused a rate of 99 and 301 and accepted 100 and 300.
- `scripts/communes/build.ts`, `src/lib/communes/`: the 565 communes of 2025 (NGI), French and Dutch names, 1,145 postcodes; search by name or postcode.
- `src/lib/professionnelle/rules.ts`: the whole spec as pure functions (requirements per profession, completeness, first incomplete step, submission, the `valide` rule, upload checks, magic-byte sniffing, who reads a file). `file.ts` loads a file; `src/lib/settings/` the students switch.
- `src/lib/documents/`: the S3 client on Neon Object Storage (presigned PUT, inspect, delete, stream) and `serveFile` behind `/api/fichiers/[id]`.
- `src/app/(portail)/espace/professionnelle/`: the space's redirect by state, the three step pages behind `openStep`, the file page, and every server action (role, state and values checked at runtime).
- `src/components/professionnelle/`: step header with progress bar, commune picker (ARIA combobox), file slot (three-move upload), the step forms, the reopening dialog. `src/components/admin/students-switch.tsx` and `src/app/(portail)/admin/`.
- `src/content/professionnelle.ts`, `src/content/admin.ts`: every word; guide lines verbatim, the rest `@relecture Surya`.
- Outside git: Neon bucket `documents` (private) on the non-production project's `main` branch, its CORS rule (PUT from uat.berceo.be, `https://berceo-*-kodominio.vercel.app`, localhost:3000), a `storage:read`+`storage:write` credential anchored there, and the five `DOCUMENTS_*` variables on Vercel Preview and `uat`. A probe proved it: presigned PUT 200, unsigned GET 403, ranged read, delete then 404.

## Acceptance criteria status

- [x] Draft → first incomplete step; four numbered steps, step 1 done, progress bar; later steps closed — `openStep` + `canOpenStep`, tests in rules.test.ts.
- [x] Partial save survives sign-out — "Enregistrer et reprendre plus tard" saves every valid field; the space sends her back to step 2.
- [x] Step 2 refuses to continue without each required field, with a message on each — `missingProfile`, tested.
- [x] Rate refused by form, server and database — `parseRate` tested; the CHECK in the migration (onboarding-migration.test.ts) and proven live on the run branch.
- [x] Bio over 500 refused, counter shows what is left.
- [x] Commune picker by French name, Dutch name or postcode; NIS codes only; one to fifty — communes.test.ts, rules.test.ts.
- [x] Students switch off / on / kept — `checkProfile` tested; the pages filter the option the same way.
- [x] `/admin` switch through the confirmation dialog, who and when recorded; non-admin 404 on the page, refused in the action.
- [x] Step 3 documents per profession, INAMI, size and type limits — tested.
- [x] Renamed executable rejected after upload and deleted — `uploadMatches` tested; `confirmUpload` deletes the object.
- [x] Replace or remove deletes object and row; a profession change drops the documents it no longer needs.
- [x] `/api/fichiers/[id]` owner and admin 200, others 404 — serve.test.ts; unsigned store URL 403 proven by the probe above.
- [x] Step 4: five declarations, five rows with version `cdc-2026-08-02`, `en_attente` + `submitted_at`.
- [x] The guide's message after submission; the space shows the pending line and the link.
- [x] Edits while `en_attente` keep the state and `submitted_at`.
- [x] `valide`: profession and documents locked behind the confirmation dialog; confirming reopens the file (D-44) so step 4 appends five rows and resubmission sets a new `submitted_at`; other fields keep `valide` — transitions tested.
- [x] Migration generated by drizzle-kit, journal ordered; a preview and UAT apply it through `vercel-build` — proven on the run branch, the preview proves it at the flip.
- [x] Commune data count and NIS uniqueness tested.
- [x] Every word in the catalogue, `@relecture` on ours; content tests over both new files.

## Notes for Release

- Not run locally, by the repo's rule: typecheck, lint, the vitest suite, the build. The Quality (advisory) job and the preview are the first to compile this branch; read them closely.
- Spec gaps decided here, in decisions.md: D-43 (`DOCUMENTS_S3_*` names, Vercel reserves `AWS_*`), D-44 (reopening a validated file goes through `brouillon`), D-45 (the photo is a document row, not `photo_file_id`), D-46 (postcode source), D-47 (the last file of a required document cannot be removed from a submitted file).
- The postcode list carries a few stray links (a postcode suggested for a neighbouring commune when OpenStreetMap placed a single locality wrong, e.g. 2223 under Anvers). NIS codes are exact; only the picker's suggestions are affected.
- Production is not set up (D41): a private `documents` bucket, its CORS rule for `https://www.berceo.be`, a credential on production's `main` branch and the five Vercel Production variables are the operator's before the batch is promoted. `env.sh audit --changed` reports those Production gaps on purpose.
- The run's Neon branch `run/onboarding-professionnelle` expires on its own in 7 days; `db-branch.sh onboarding-professionnelle down` releases it at close-out.
- Context budget: over the Inputs table. Neon object-storage docs, the Vercel project's environment list, the Eurostat LAU file and the NGI and bpost-derived datasets were read to build the storage and the register.
