# Handoff: onboarding-professionnelle

For the next session — human or agent — what to do first and what stands in the way.
Rewritten, not appended, at every stage stop; a stage that STOPs mid-way writes it before it
stops, so nothing is carried in anyone's head.

## Next steps

1. Merged and archived; nothing to pick up in this run.
2. Before the batch's promotion: the production bucket's CORS rule (PUT from `https://www.berceo.be`)
   — the operator adds the production storage credential to the cloud environment as
   `PROD_DOCUMENTS_S3_ACCESS_KEY_ID` / `PROD_DOCUMENTS_S3_SECRET_ACCESS_KEY` and a session sets it.

## Blockers

- none for this run.

## Do not

- Do not write to the production Neon project from a run without the operator's word (D41).
- Do not print or commit a storage credential.
