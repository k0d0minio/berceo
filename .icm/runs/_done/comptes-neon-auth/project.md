# Project: comptes-neon-auth

The run's context card — what a fresh session needs before it reads anything else. Pointers,
not copies: the spec stays the spec, the scope stays the scope. Seeded when the run is opened
(`new-run.sh` → `run-pack.sh --init`), sharpened by whichever stage learns something. Read with
`status.md` and `handoff.md` on every resume (`_shared/stage-preamble.md`).

- stub: intake/plateforme-v1/comptes-neon-auth.md
- scope: .icm/runs/plateforme-v1/01_scope/output/scope.md
- spec: 02_define/output/spec.md
- touches: src/db/schema.ts, drizzle/**, src/db/**, src/app/(auth)/**, src/app/(portail)/**, src/app/api/auth/**, src/app/api/webhooks/**, src/lib/auth/**, src/lib/email/**, src/content/comptes.ts, src/content/emails.ts, src/content/portal.ts, src/proxy.ts, scripts/grant-admin.ts, package.json, package-lock.json, .env.example, README.md, AGENTS.md, .icm/_shared/project-rules.md
- complexity: complex → model: opus (executor — select-model.sh --stage 03_build)

## Constraints

- Every run merges into `uat` (D-22); this branch was cut from `origin/uat` with `origin/main` merged in.
- Words only in `src/content/`, guide verbatim or `@relecture Surya` (D-19); never "assurance Berceo" (D-8).
- One forward-only migration, generated by drizzle-kit, never hand-edited (`_shared/project-rules.md` → Migrations).
- No address, key or cookie secret in git; `EMAIL_FROM` is the operator's (D-28).
- Neon `main` is production: Build changes its auth config (verification, Google off, webhook, trusted domains) and nothing else there; no SQL writes.
- The decisions this run made are D-28 to D-35 in `decisions.md`.

## Context budget

- Define read beyond its Inputs: the guide's "Les comptes" and "Les e-mails" sections (verbatim copy), four Neon Auth docs pages (webhooks, custom e-mails, branching, Next.js server SDK), and live reads of Neon (auth config, branches, schema) and Resend (domains).
