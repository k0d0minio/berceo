# Handoff: plateforme-v1

For the next session — human or agent — what to do first and what stands in the way.
Rewritten, not appended, at every stage stop; a stage that STOPs mid-way writes it before it
stops, so nothing is carried in anyone's head.

## Next steps

1. Review `01_scope/output/scope.md` (the 27 decisions, the out-of-scope list, the 11 open points) and `.icm/intake/plateforme-v1/breakdown.md` on the PR; merge it so the board sees the batch.
2. Then `new` — it picks `socle-design-system` (sequence 1 of 15) and walks it into Define.
3. Before `socle-design-system` builds: fetch the Comodo font files and licence from Surya's Drive; before `comptes-neon-auth`: decide the e-mail sending domain and add the DNS records.

## Blockers

- none for the first stubs. Prices (subscriptions) and the identity check are out of this scope by decision, not blocked.

## Do not

- Do not edit `01_scope/_source/story.md`; it froze on the push.
- Do not patch `scope.md` in place; a wrong scope is deleted and Scope re-run from the source.
- Do not tick the gates; do not run `new` for the operator.
