# Build notes: avis-etoiles

- commits: 68ec440 schema, migration and rules · 19efff1 data module · 690aa53 rating flow and note surfaces · 4a94fa3 invitations, founders' list, design system, docs · the no-text tests and these notes
- ci: GREEN on the full gate (ready) at 7698ad2: the Vercel preview built, applied `0011_avis` to its own Neon branch and passed `db:verify`. The advisory quality job passed on d396c3e, the last code commit.

## The garde facts this run reads (plan Pass 0)

cycle-de-garde-et-annulation merged as #48. The state is computed, never stored (its D-109).

- **terminée**: `gardeState(facts, now) === "terminee"` in `src/lib/gardes/rules.ts`, where `facts = { status, nightDate, startTime }`. `bookings.status` is `confirmee | annulee`; `startTime` comes from `care_requests.start_time`.
- **annulée**: `bookings.status = 'annulee'`. This covers both a cancellation before the start and an absence reported until end + 24 h (`cancellation_kind = 'absence'`).
- **the end instant**: `nightEnd(nightDate, startTime)` in `src/lib/gardes/rules.ts` (Brussels wall clock, start + `NIGHT_HOURS` = 11). In SQL it is `(r.night_date + r.start_time + interval '11 hours')` compared with `now() AT TIME ZONE 'Europe/Brussels'`, the form `src/lib/gardes/gardes.ts` already uses.

A terminée garde can still turn annulée by an absence within 24 h of its end. This run holds that as D-122.

## What changed

- `src/db/schema.ts`, `drizzle/0011_avis.sql`: the `rating_side` enum, `ratings` (four smallint scores checked 1–5, unique garde and side, no text column) and `rating_invitations` (the claim of one side's e-mail). Generated by drizzle-kit, applied and `db:verify`-ed on the run's Neon branch `run/avis-etoiles`.
- `src/lib/avis/rules.ts` (+ tests): the criteria, the 14-day window, `rateRefusal`/`canRate`, `isPublished`, the note and its comma, `parseScores`. It reads the garde's state through `gardeState`.
- `src/lib/avis/ratings.ts`: every read and write of the two tables. Every rule is held again in SQL: the insert (own garde, terminée, not annulée, window open, `on conflict do nothing`), the note (published only, the garde still confirmed, D-122), the gardes count (terminée, not annulée), the invitations owed and their claim.
- `src/lib/avis/notify.ts` (+ tests), `src/app/api/cron/avis-invitations/` (+ tests), `.github/workflows/avis-invitations.yml`: the hourly invitation, claim before the send and release on failure (the learned rule from cycle-de-garde), idempotency key per garde and side.
- `src/lib/email/templates.ts` (+ tests), `src/content/emails.ts`: the guide's family e-mail verbatim, the professional's, and the 14-day line.
- `src/components/avis/`: `NoteDisplay`, `GivenRating`, `OwnNote`, `StarInput`/`RatingForm` (native radios, visually hidden, 44 px targets), `RatingScreen`, `RatingsTable`, `criteriaLabels`.
- The two form routes with their actions, « Laisser un avis » on both lists and both garde pages, the given stars on both garde pages.
- The note on the full profile (`PublicProfile.note`), the answer cards (`Applicant.note`), the professional's request cards (`ProfessionalRequest.family`, keyed by request id so the family's id never leaves `src/lib/avis/`), her garde page, and both homes.
- `/admin/avis` with the admin home link and count; `/design-system/portail` shows the stars; README « The ratings »; AGENTS.md routing and data-model rows.
- `src/content/avis.ts` (+ test), `src/content/admin.ts`, `src/content/design-system.ts`: every word, `@relecture Surya` apart from the guide's quotes.

## Acceptance criteria status

Recorded here rather than ticked on the PR (a learned rule: a session's ticks there are refused as self-approval). "Proved on Neon" means a throwaway probe against the run's Neon branch, deleted before committing.

- [x] Family rates from « Mes réservations » and sees her stars on her booking page — built; the 360 px check is the operator's smoke on the preview.
- [x] The professional does the same from « Mes gardes » with her four criteria — built; smoke on the preview.
- [x] No textarea, no text input, no text column — `src/lib/avis/no-text.test.ts` renders the form and reads the table config.
- [x] The server action refuses another user's booking, not terminée, annulée, closed window, a second rating, and bad scores; forged fields are ignored — `rules.test.ts` covers `parseScores` and the refusals; the SQL was proved on Neon (introuvable, pasTerminee, annulee, delaiPasse, dejaNote).
- [x] At most once per side; two concurrent submissions store one — proved on Neon (`Promise.all` of two inserts: one ok, one dejaNote).
- [x] « Laisser un avis » only when `canRate`; outside, the form route shows the reason line — `RatingScreen`.
- [x] Double-blind: one side only counts nowhere; both, or the window closed, count — proved on Neon; `isPublished` tests.
- [x] The professional's note on her profile and the answer cards: the mean to one decimal with a comma, « Pas encore de note » otherwise — proved on Neon (4,5); rounding tests.
- [x] The gardes count: terminée, non-annulée, rated or not, hidden at zero — proved on Neon, including a garde cancelled by an absence after its end dropping out.
- [x] The family's note and count on each request card (no identity) and on her garde page.
- [x] Each side's own note on its home; nobody reads the other side's scores — only `adminRatings` returns single ratings.
- [x] The hourly pass: the family gets the guide's e-mail verbatim, the professional hers, each to its form, once per side; not to a side that has rated, not for a closed window, not for an annulée garde — `notify.test.ts`, `templates.test.ts`; `invitationsDue`/`claimInvitation` proved on Neon.
- [x] The route refuses without `CRON_SECRET`; the workflow calls uat and production hourly and skips on a missing secret — `route.test.ts`, the workflow.
- [x] `/admin/avis`: every rating, newest first, 50 a page, the garde, both names and roles, the labelled scores, the mean and the state; linked from `/admin`. **Differs from the spec's wording**: a parent or professional gets the 404 every `/admin` path gives them (D-33), not a redirect to their space; `routing.test.ts` holds the 404.
- [x] Star input by keyboard (native radios: Tab between criteria, arrows within), 44 × 44 px targets, accessible names; the note reads « Note X,Y sur 5 » — to confirm on the preview with a screen reader.
- [x] Unit tests on the rules module: the window edges, including across the change to winter time; each refusal; the three publication paths; the rounding (4,95 → 5,0; single rating); score validation.
- [x] The migration on the preview's own Neon branch with `db:verify`: generated by drizzle-kit, verified on `run/avis-etoiles`, and applied by the ready preview build (green).
- [x] Every word in `src/content/`, `@relecture` unless quoted, `avis.test.ts`.
- [x] `routing.test.ts` covers both form routes and `/admin/avis`.
- [x] `/design-system/portail` shows the star input, a sample note and the no-note state.
- [x] README « The ratings » and the AGENTS.md row.

## Notes for Release

- **D-122, a spec gap:** cycle-de-garde lets either side report an absence until 24 h after the end, so a terminée garde can turn annulée after it was rated. Its ratings stay stored and show on `/admin/avis` as « Garde annulée, ne compte pas ». They count in no note and the garde in no count, and the form and invitation are refused from then on. Review whether the founders want that.
- **The admin 404:** the spec said a non-admin on `/admin/avis` is "sent to their own space". The repo's rule (D-33) answers 404 on every `/admin` path, and Build kept that.
- **Decision ids renumbered:** D-105–D-111 → D-115–D-121 (cycle-de-garde merged first with D-105–D-114). D-122 is Build's.
- **The first deploy:** every garde that ended in the last 14 days is owed an invitation on the first pass after promotion (the spec excludes only closed windows). On UAT that is test gardes only.
- **Review closely:** the raw SQL in `src/lib/avis/ratings.ts` (the `isPublishedRating` subquery refers to the outer `ratings` row; the insert-select holds every rule) and the request-card note keyed by request id.
- No linter or formatter is wired for this project (`lint.sh`, `format.sh` → SKIP); the advisory quality job after the flip is the first typecheck and test run.

Context budget: read `src/lib/gardes/` (rules, gardes, notify), the reminder route and workflow, the e-mail templates and their tests, and the pages each surface touches, beyond the Inputs table; the spec's touches named the pages, and the garde state is that module's.
