import { SPACES } from "@/lib/auth/routing";

/** « Mes disponibilités », inside the professional's space (and so behind its guard). */
export const AVAILABILITY_PATH = `${SPACES.professionnel}/disponibilites`;
