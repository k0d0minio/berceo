/*
 * The browser-chrome colours, for `viewport.themeColor`.
 *
 * The one exception to "colours live in globals.css": a <meta name="theme-color">
 * cannot read a CSS variable, so the value has to be written out. Each mirrors a
 * token in globals.css; change both together.
 */

/** The DA's --blanc. */
export const themeColorJour = "#ffffff";

